# Database data directory - contains sensitive data, should not be committed
data/mysql/

# Backup files (except database backups which are tracked)
*.sql
backup.sql

# Include database backups
!db_backup_*.sql.gz

# Other common ignores
.env
.DS_Store
*.log
node_modules/
# Archivo de ejemplo de variables de entorno para DTIC Bitácoras
# Copia este archivo como .env y ajusta los valores según tu entorno

# === CONFIGURACIÓN GENERAL ===
NODE_ENV=development
# Opciones: development, production, test

# === BASE DE DATOS POSTGRESQL ===
POSTGRES_DB=dtic_bitacoras
POSTGRES_USER=dtic_user
POSTGRES_PASSWORD=dtic_password
# ⚠️  IMPORTANTE: Cambia esta contraseña en producción!
POSTGRES_PORT=5432

# URL completa de conexión a la base de datos
DATABASE_URL=postgresql://dtic_user:dtic_password@postgres:5432/dtic_bitacoras

# === API BACKEND ===
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# ⚠️  IMPORTANTE: Genera una clave secreta segura para producción!
# Puedes generar una con: openssl rand -base64 32
API_PORT=3001

# === FRONTEND ===
VITE_API_URL=http://localhost:3001/api
FRONTEND_PORT=5173

# === CONFIGURACIÓN DE RED ===
NETWORK_NAME=dtic_network

# === VOLÚMENES ===
POSTGRES_VOLUME_NAME=dtic_bitacoras_postgres_data

# === CONFIGURACIÓN PARA PRODUCCIÓN ===

# Para despliegue en producción, ajusta estas variables:
# NODE_ENV=production
# POSTGRES_PASSWORD=tu_password_seguro_aqui
# JWT_SECRET=tu_clave_jwt_segura_aqui
# VITE_API_URL=https://tu-dominio.com/api
# POSTGRES_PORT=5432
# API_PORT=3001
# FRONTEND_PORT=80

# === CONFIGURACIÓN SSL (OPCIONAL) ===
# Si usas HTTPS en producción:
# SSL_CERT_PATH=/path/to/ssl/cert.pem
# SSL_KEY_PATH=/path/to/ssl/private.key

# === LOGGING ===
LOG_LEVEL=info
# Opciones: error, warn, info, debug

# === RATE LIMITING ===
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# === CONFIGURACIÓN DE EMAIL (OPCIONAL) ===
# Para funcionalidades de notificación:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=tu-app-password